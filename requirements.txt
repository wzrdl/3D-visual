# ---- Core scientific stack ----
numpy>=1.24,<2.2          # 稳定版本，兼容多数科学库

# ---- Qt & 3D visualization stack ----
PyQt6>=6.6.0,<6.10        # 使用 Qt6，避免过新版本的潜在不兼容
PyQt6-Qt6>=6.6.0,<6.10
PyQt6-sip>=13.6.0

pyvista>=0.43.0,<1.0      # 3D/VTK 可视化
pyvistaqt>=0.11.3,<0.12   # 与 PyQt6 + QtPy 协同工作
QtPy>=2.4.0,<3.0          # 统一 Qt 抽象层，供 pyvistaqt 使用

# ---- Networking / async HTTP ----
httpx>=0.25.0,<0.28

# ---- 可选：常用工具（如不需要可删除）----
# tqdm>=4.65.0            # 进度条
# pydantic>=2.0           # 配置/数据模型
# rich>=13.0              # 终端彩色输出
